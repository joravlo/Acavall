<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Entrada</title>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jspdf/1.3.2/jspdf.min.js"></script>
    <script src="http://code.jquery.com/jquery-latest.js"></script>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
  </head>
  <body>
    <style media="screen">
    .contenido-vista {
      width: 80%;
      margin-left: auto;
      margin-right: auto;
      margin-top: 5%;
      background-color: #ededed;
    }

    body {
      background-color: #ededed;
    }

    .entras {
      text-align: center;
      font-weight: bold;
      font-size: 17px;
    }

    .pago {
      font-size: 25px;
      color: green;
    }

    .entras span {
      color: green;
      font-size: 30px;
    }

    table {
      border-collapse: collapse;
    }

    table, td, th {
      border: 5px solid lightgrey;
    }

    #content {
      visibility: collapse;
      background-color: #ededed;
      width: 60%;
    }

    </style>

    <div class="entras">
      <br><br>
      <p class="pago">Enhorabuena, el pago se ha realizado con exito.&nbsp;&nbsp;&nbsp;<span class="glyphicon glyphicon-ok"></span></p>
      <p> A continuacion tienes la entrada para el evento.Tambien se le a enviado un correo con la entrada</p>
      <button id="cmd" type="button" class="btn btn-primary" style="background-color: #c8c22f; border:none; padding: 15px 70px; font-weight:bold;">Guarda tu entrada</button>
    </div>
    <div id="ticket" class="contenido-vista">
    <table class="table table-sm">
        <tr>
          <td colspan="2"><img src="{{asset('img/imagesEvents/' ~ event.image)}}" alt="Foto Entrada" height="150" width="300" style="float:left"><p style="margin-left: 80px; margin-top: 50px; font-size: 35px; font-weight:bold; float:left">{{event.name}}</p></td>
        </tr>
      <tbody>
        <tr>
          <td><h5 style="color:#6b6663;">Fecha + hora </h5><p style="text-align: center; font-size: 15px; font-weight:bold;">{{event.date|date('Y-M-d')}}</p></td>
          <td><h5 style="color:#6b6663;">Ubicacion</h5><p style="text-align: center; font-size: 15px; font-weight:bold;">{{event.address}}</td>
        </tr>
        <tr>
          <td colspan="2"><h5 style="color:#6b6663;">Informacion del pedido</h5><p style="text-align: center; font-size: 15px; font-weight:bold;">Entrada nº{{ticket.numTicket}}. Pedido por {{user.name}} {{user.surname}}</p></td>
        </tr>
        <tr>
          <td colspan="2"><h5 style="color:grey;">Tipo de entrada</h5><p style="text-align: right; font-size: 15px; font-weight:bold; ">Entrada normal {{event.price}}€</p></td>
        </tr>
      </tbody>
    </table>
        </div>
        <script type="text/javascript">



$('#cmd').click(function() {
  var options = {
  };
  var pdf = new jsPDF('landscape', 'mm', [200, 200]);
  pdf.addHTML($("#ticket"), 0, 0, options, function() {
    pdf.save('entradaAcavall.pdf');
  });
});

</script>
  </body>
</html>
